<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yourtrip.md.mapper.YourStayMapper">
	<!-- <select id="selectPerPage" resultType="YourTrip" parameterType="PageVO">
		<![CDATA[
	     select * from (select ROWNUM rnum, aa.* from (select * from accommodation order by aid desc) aa)
	     where rnum>#{startRow} and rnum<=#{endRow}
	     ]]>
	</select>
	<select id="selectBySeq" resultType="YourTrip" parameterType="long">
		select * from board where SEQ =#{seq}
	</select>
	<select id="selectCount" resultType="long">
		select max(ROWNUM) from BOARD
	</select>
	<insert id="insert" parameterType="YourTrip">
	    insert into BOARD values(BOARD_SEQ.nextval, #{writer}, #{email}, #{subject}, #{content}, SYSDATE, #{fname}, #{ofname}, #{fsize})
	</insert>
	<update id="update" parameterType="YourTrip">
		update BOARD set EMAIL=#{email}, SUBJECT=#{subject}, CONTENT=#{content} , FNAME=#{fname} where SEQ = #{seq}
	</update>
	<delete id="delete" parameterType="long">
		delete from BOARD where SEQ = #{seq}
	</delete>-->
	<!-- for Ajax 
	<select id="selectBySeq2" resultType="YourTrip" parameterType="long">
		select * from BOARD where SEQ=#{seq}
	</select>
	<select id="selectByName" resultType="YourTrip" parameterType="String">
		select * from Board where WRITER like '%'||#{writer}||'%' order by WRITER
	</select> -->
	<!-- <select id="searchSubject" resultType="Board" parameterType="long">
		select * from board where subject like '%#{subjectRow}%'
	</select> -->
</mapper>
<!-- resultType : 결과반환 Object / return 값이라고 생각하면 될 듯?
	 parameterType : 조회결과 Object
 -->